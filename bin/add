#!/bin/bash

add() {
    local numbers
    split_into "$1" numbers
    print_sum_of "${numbers[@]}"
}

split_into() {
    IFS=, read -ra "$2" <<< "$1"
}

print_sum_of() {
    local sum=
    for n in "$@"; do
        ((sum+=n))
    done
    echo "$sum"
}

[[ "$BASH_SOURCE" == "$0" ]] && add "$@"
