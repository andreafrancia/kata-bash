#!/bin/bash
adder() {
    local text="$1"

    IFS=, read -ra numbers <<< "$text" 

    local sum=0
    for number in "${numbers[@]}"; do
        ((sum+=number))
    done
    echo "$sum"
}

[[ "${BASH_SOURCE[0]}" == "$0" ]] && adder "$@"
